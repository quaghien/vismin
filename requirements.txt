transformers
diffusers
huggingface-hub
kornia
albumentations
absl-py
accelerate
# autoawq
diffusers
flair
# fastapi
gradio
huggingface-hub
img2dataset
matplotlib
numpy
pandas
ray
scikit-learn
torch
torchvision
transformers
uvicorn
wandb
xformers
Levenshtein
spacy
openai
datasets
vllm
python-dotenv
google-generativeai
easydict
inflect
hydra-core

# python -m spacy download en_core_web_sm

#transformers==4.31.0


# ============================================================================
# INSTALLATION STEPS (Chạy theo thứ tự sau khi pip install -r requirements.txt)
# ============================================================================

# Step 1: Install fairseq from conda-forge (tránh build errors với pip)
# conda install -c conda-forge fairseq -y

# Step 2: Install TIFA dependencies (cần install riêng vì conflict versions)
# pip install word2number evaluate salesforce-lavis promptcap 'modelscope[multi-modal]' torchaudio

# Step 3: Install TIFA from GitHub (KHÔNG dùng PyPI package 'tifa')
# git clone https://github.com/Yushi-Hu/tifa.git && cd tifa && pip install -e . --no-deps

# cp -r /home/taquocthai/hienhq/vismin/tifa/tifascore /home/taquocthai/miniconda3/envs/vismin2/lib/python3.10/site-packages/

# Step 4: Reinstall torch/torchvision (vì bị uninstall khi cài TIFA)
# pip install torch torchvision

# Step 5: Upgrade numpy, pyarrow và datasets để tương thích
# pip install 'numpy>=1.24.4' pyarrow datasets --upgrade

#python -c 'from tifascore.vqa_models import VQAModel; print("TIFA import thành công")'

# ============================================================================
# LỖI THƯỜNG GẶP VÀ CÁCH FIX:
# ============================================================================
# - ImportError "UnifiedQAModel": Code đã fix, dùng VQAModel thay vì UnifiedQAModel
# - fairseq build error: Dùng conda install thay vì pip
# - "libtorch_cuda.so not found": Reinstall torch/torchvision sau khi cài TIFA
# - numpy._core error: Upgrade numpy lên >= 1.24.4 (sẽ lên 2.2.6)
# - pyarrow.PyExtensionType error: Upgrade datasets lên 4.5.0
# - Python 3.11 dataclass errors: Nên dùng Python 3.10 (đã test OK)
# - modelscope/vllm warning về transformers version: Bỏ qua, vẫn chạy được
# - transformers 5.0.0 incompatible: Downgrade về 4.34-4.37 và upgrade vLLM
# - vLLM không support Mistral: Upgrade vLLM lên >= 0.3.0

# ============================================================================
# CÁCH BỎ QUA STAGE 2.5 (NẾU KHÔNG CẦN AUTO-FILTERING):
# ============================================================================
# Stage 2.5 chỉ cần cho COCO/VSR để auto-filter bad generated images
# Nếu bạn muốn skip:
# 1. Chạy Stage 1, 2, 3 như bình thường
# 2. Bỏ qua Stage 2.5 (auto_filter_question_gen.py)
# 3. Stage 3 sẽ generate TẤT CẢ images (không filter)
# 4. Manual review/filter sau nếu cần
# 
# Ưu điểm: Không cần cài TIFA (tránh dependency hell)
# Nhược điểm: Có thể có ảnh chất lượng kém trong output
# ============================================================================
